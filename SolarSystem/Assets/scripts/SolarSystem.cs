using UnityEngine;
using System.Collections;

public class SolarSystem : MonoBehaviour {
	

	// Use this for initialization
	void Start () {
//		earth=GameObject.Find("earth").gameObject;
//		sun=GameObject.Find("sun").gameObject;
//		Debug.Log (earth.name+" " +sun.name);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
